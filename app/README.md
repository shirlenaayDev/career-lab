# Career Lab — Web Application

React + Vite frontend for Career Lab, connected to Supabase backend.

## Setup

```bash
npm install
npm run dev
```

## Environment Variables

Create a `.env` file with:
```
VITE_SUPABASE_URL=your_project_url
VITE_SUPABASE_ANON_KEY=your_anon_key
```